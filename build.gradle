apply plugin: 'java'
apply plugin: 'application'

archivesBaseName = "Event-Planner"
version = '3.0'
mainClassName = "App"

repositories {
 mavenCentral()
}

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
}
